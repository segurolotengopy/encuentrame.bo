{
  "name": "@encuentrame/functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=lib/index.js --external:firebase-admin --external:firebase-functions --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo functions:lint-ok",
    "test": "echo functions:no-tests",
    "serve": "pnpm build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@encuentrame/shared": "workspace:*",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "esbuild": "^0.24.0"
  }
}